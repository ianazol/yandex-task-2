{{#each items}}
    <b>{{name}}</b><br/>
    <span class="text-muted small">{{date}} ({{startTime}} - {{finishTime}})</span><br/>
    Лектор: {{lecturer}}<br/>
    Школа: {{schoolList}}<br/>
    Аудитория: {{classroom.name}}
    <hr/>
{{/each}}